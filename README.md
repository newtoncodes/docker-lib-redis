# Redis on ubuntu docker image

Basic redis image, no additional functionality. 

You need to pass environment variable **ROOT_PASSWORD** on every run.

You can also use a secret pointing it to **/etc/redis/passwd**.
